#!/bin/bash

# Destination device, e.g. /dev/sda
DESTINATION_DEVICE=

# Hostname
HOSTNAME=home

# City (for timezone)
CITY=Melbourne

# Country (for timezone and fastest pacman mirror)
COUNTRY=Australia

function log_progress {
  echo -e "\n"[$(date +"%d/%m/%Y %k:%M:%S")]: $1
}

